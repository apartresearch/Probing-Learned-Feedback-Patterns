class ProjectConfigs:
    """
    Gives wandb configs for the RLHF model training,
    for the autoencoder training, the reconstructions,
    and finally the linear probes. This specifies the wandb project names used for each stage of the pipeline.
    """